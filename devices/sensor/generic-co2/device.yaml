# yaml-language-server: $schema=https://growrig.dev/schema/catalog/device.json
brand: Generic
model: "CO₂ sensor"
connection: generic
provides:
  - label: "CO₂"
    kind: sensor
    measurement: co2
    entityDomain: sensor
    deviceClass: carbon_dioxide
